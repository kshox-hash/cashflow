NODE_ENV=development
PORT=3001

# JWT — genera uno seguro con: node -e "require('crypto').randomBytes(64).toString('hex')"
JWT_SECRET=

# CORS — dominio del frontend (sin trailing slash)
CORS_ORIGIN=http://localhost:5173

# PostgreSQL
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=
PGDATABASE=floatapp
